import { ItemFilterPipe } from './item-filter.pipe';

describe('WorkflowFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ItemFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
