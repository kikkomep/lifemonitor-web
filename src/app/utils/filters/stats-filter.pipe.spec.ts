import { StatsFilterPipe } from './stats-filter.pipe';

describe('WorkflowStatsFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new StatsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
