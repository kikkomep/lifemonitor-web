import { SortingFilterPipe } from './sorting-filter.pipe';

describe('SortingFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SortingFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
